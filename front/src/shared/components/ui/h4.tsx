export default function H4({ children }: { children: React.ReactNode }) {
  return (
    <h4 className="text-sm font-semibold tracking-wider text-amber-600 uppercase">
      {children}
    </h4>
  );
}
